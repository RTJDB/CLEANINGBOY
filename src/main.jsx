import React from "react";
import ReactDOM from "react-dom/client";
import TourDashboard from "./components/TourDashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TourDashboard />
  </React.StrictMode>
);
